/**
 * 
 */
package com.mcp.scheduler.mq.consumer;

/**
 * @author ming.li
 *
 */
public class McpMessageConsumer {
	
	/*@Autowired
	private MsgHandler mcpMsgHandler;
	
	private static Logger log = Logger.getLogger(McpMessageConsumer.class);
	
	public void consume(McpMessage msg)
	{
		try {
			mcpMsgHandler.handler(msg);
		} catch (Exception e) {
			e.printStackTrace();
			log.error("msgerror----" + msg.toString());
		}
	}*/
}
